# Flyway Configuration File
# Picks up environment variables to maintain single source of truth

flyway.url=jdbc:postgresql://localhost:5432/codegen
flyway.user=postgres
flyway.password=Password@123
flyway.cleanDisabled=false
